# Dependencies for the scheduled export jobs (fetch_klar_export.py and
# refresh_matrixify_mcp.py).
pandas>=2.2
requests>=2.32
openpyxl>=3.1
# Matrixify MCP refresh (refresh_matrixify_mcp.py):
mcp>=1.2
httpx>=0.27
